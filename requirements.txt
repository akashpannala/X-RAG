# Phase 1 pins — run: uv pip install --python .venv/bin/python -r requirements.txt
# NOTE: install torch from CPU index first:
#   uv pip install --python .venv/bin/python --index-url https://download.pytorch.org/whl/cpu torch
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
httpx>=0.27
rich>=13.0
qdrant-client>=1.9
kuzu>=0.6
watchdog>=4.0
datasketch>=1.6
bm25s>=0.3
sentencepiece>=0.2
pytest>=8.0
docling>=2.0
sentence-transformers>=3.0
torch
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
# Phase 4 (L22 RAGOps) — verified versions on this box
redis>=8.0,<9
prometheus-client>=0.26,<1
langfuse==4.15.3
# langchain stack (offline-safe once installed)
langchain-core==1.6.3
langchain-groq==1.1.3
langchain-ollama==1.1.0
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
langgraph==1.2.11
PyJWT==2.14.0
passlib==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 reads bcrypt.__about__ (gone in bcrypt>=4.1)
numpy==2.4.6
# ragas pinned but import-broken vs sunset langchain-community 0.4.2;
# l21_eval falls back to judge aggregates until upstream stabilizes
ragas==0.4.3
datasets==5.0.1
